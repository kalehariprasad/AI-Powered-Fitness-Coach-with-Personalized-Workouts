aiohttp==3.11.16          # Remove if not used
altair==5.5.0             # Used by Streamlit or visualizations
fpdf==1.7.2               # Required for generating PDFs
huggingface-hub==0.30.2    # Required for Hugging Face API access
langchain==0.3.23         # Required for LangChain-based processing
langchain-huggingface==0.1.2 # Required for Hugging Face integration
langchain-core==0.3.51    # Core LangChain functionality
numpy>=1.26,<2.0          # Required for numerical operations
pandas==2.2.3             # Remove if not working with dataframes
protobuf==5.29.4          # Required by Hugging Face
requests==2.32.3          # HTTP library (if needed)
streamlit==1.44.1         # Required for the app frontend
torch==2.6.0              # Remove if not using deep learning models
transformers==4.51.1      # Remove if not using Hugging Face models
